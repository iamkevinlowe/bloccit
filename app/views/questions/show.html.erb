<h1><%= @question.title %></h1>

<%= link_to "Edit", edit_question_path(@question), class: 'btn btn-success' %>

<h4><%= @question.resolved ? "Question has been resolved." : "Still looking for an answer." %></h4>

<p><%= @question.body %></p>